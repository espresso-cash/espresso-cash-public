name: jsonrpc_client
description: A starting point for Dart libraries or applications.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  analyzer: ">=5.4.0 <8.0.0"
  build: ^2.1.1
  http: ^1.1.0
  solana:
    path: ../solana
  source_gen: ">=1.4.0 <3.0.0"

dev_dependencies:
  mews_pedantic: ^0.27.0
  test: ^1.16.0
