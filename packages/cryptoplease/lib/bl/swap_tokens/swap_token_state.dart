part of 'swap_token_bloc.dart';

@freezed
class SwapTokenState with _$SwapTokenState {
  const factory SwapTokenState({
    JupiterIndexedRouteMap? routeMap,
  }) = _SwapTokenState;

  const SwapTokenState._();
}
