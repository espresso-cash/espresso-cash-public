import 'package:flutter/material.dart';

class PayerNameScreen extends StatelessWidget {
  const PayerNameScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => Container(
        color: Colors.white,
      );
}
