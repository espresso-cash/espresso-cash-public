import 'package:flutter/material.dart';

class EcDivider extends StatelessWidget {
  const EcDivider({super.key});

  @override
  Widget build(BuildContext context) => const Divider(
        color: Color(0xff3F3C3D),
        thickness: 1,
      );
}
